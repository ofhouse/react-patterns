import { MenuList as ReachMenuList } from '@reach/menu-button';
import styled from 'styled-components';

export const DropdownMenu = styled(ReachMenuList)({
  position: 'absolute',
  border: '1px solid #E5E5E5',
});
